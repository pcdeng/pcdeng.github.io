let selectedDevice,writeChatt,notifyChatt;let SERVICE_UUID="000018f0-0000-1000-8000-00805f9b34fb",WRITE_CHATT_UUID="00002af1-0000-1000-8000-00805f9b34fb",NOTIFY_CHATT_UUID="xxxxxxxx",send=e=>{if(!writeChatt){console.log("无法打印：打印特性属性为空");return}let t=new TextEncoder().encode(e);return writeChatt.writeValue(t).then(()=>{console.log("发送完毕")})},read=e=>new Promise(async(t,n)=>{let a;await send(e);let i=async e=>{a&&clearTimeout(a),t(e.target.value),await notifyChatt.stopNotifications()};await notifyChatt.startNotifications(),notifyChatt.addEventListener("characteristicvaluechanged",i),a=setTimeout(()=>{n(Error("Timeout"))},5e3)}),initBle=async()=>selectedDevice?writeChatt&&notifyChatt?Promise.resolve({writeChatt,notifyChatt}):Promise.resolve(selectedDevice).then(e=>(console.log(`设备名称 ${e.name}`),console.log("连接到 GATT 服务器......"),e.gatt.connect())).then(e=>e.getPrimaryService(SERVICE_UUID)).then(async e=>(writeChatt=await e.getCharacteristic(WRITE_CHATT_UUID),notifyChatt=await e.getCharacteristic(WRITE_CHATT_UUID),{writeChatt,notifyChatt})):Promise.reject(Error("没有配对设备")),handleSend=()=>{initBle().then(()=>{send("SELFTEST\r\n")}).catch(e=>{console.log("发送指令失败：",e.message)})},pair=async()=>navigator.bluetooth.requestDevice({filters:[{services:[SERVICE_UUID]}]}).then(e=>(selectedDevice=e,e)).catch(e=>(console.log(e.message),"")),handleConnect=async()=>{console.log("配对设备：",await pair())},init=()=>{document.getElementById("connectBtn").addEventListener("click",handleConnect),document.getElementById("sendBtn").addEventListener("click",handleSend)};init();